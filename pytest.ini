[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: fast unit tests (no full HTTP stack required beyond imports)
    integration: API tests via FastAPI TestClient
    e2e: end-to-end HTTP flows (create → list → comment)
